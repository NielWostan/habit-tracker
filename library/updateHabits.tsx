export function updateHabits(
  data: any[],
  operation: string,
  habitId?: number,
  id?: number,
  date?: string
) {
  switch (operation) {
    case "add": {
      const returnData = [];
    }
  }
}
// 1. add a habit
// 2. delete a habit
// 3. add progress
// 4. drop progress
